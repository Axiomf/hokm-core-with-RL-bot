#include "classes.hpp"
Card easy_bot(GameState& game){
    GameState::State game = game.get_state();
    if (game.played1.get_rank() == 0) // bot starts the round
    {
        
    }
    else
    {
        if game.played1.get_rank() == 0
    }
    
    
    
}